<div class="offcanvas offcanvas-end" data-bs-scroll="true" data-bs-backdrop="false" tabindex="-1" id="offcanvas" aria-labelledby="offcanvasLabel">
    <div class="offcanvas-header">
        <button type="button" class="btn-close text-reset ms-auto" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body text-center">
        <div class="mb-3">
            <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button class="btn btn-primary d-block w-100" id="menuButton" type="submit" >Ustawienia</button>
            </form>
        </div>
        <div class="mb-3">
            <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button class="btn btn-primary d-block w-100" id="menuButton" type="submit" >Ogłoszenia</button>
            </form>
        </div>
        <div class="mb-3">
            <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button class="btn btn-primary d-block w-100" id="menuButton" type="submit" >Obserwowane</button>
            </form>
        </div>
        <div class="mb-3">
            <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button class="btn btn-primary d-block w-100" id="menuButton" type="submit" >Wyloguj</button>
            </form>
        </div>
    </div>
</div>